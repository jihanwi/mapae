# GIWA Sepolia testnet RPC
GIWA_SEPOLIA_RPC_URL=https://sepolia-rpc.giwa.io

# Blockscout verifier endpoint (no API key required)
BLOCKSCOUT_API_URL=https://sepolia-explorer.giwa.io/api

# NOTE: Never put private keys in .env files.
# Use an encrypted keystore instead:
#   cast wallet import deployer --interactive
# Then deploy with: forge script ... --account deployer
